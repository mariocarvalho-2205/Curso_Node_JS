<h2>Usuarios Cadastrados</h2>
<div class="container">

  {{#each users}}
    <div class="item">
        <h4>{{this.name}}</h4>
        <p>{{this.ocupation}}</p>
        <div class="actions">
            <a href="/users/{{this.id}}">Ver detalhes</a>
        </div>
    </div>
  {{/each}}
</div>